package utils;

import org.openqa.selenium.By;
import org.openqa.selenium.JavascriptExecutor;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;

public class ProductId {
/*
    protected  WebDriver driver;

    public  String getProductId (WebDriver driver, String path) {

     */
/* String innerText = driver.findElement(By.xpath("//li[@class='gallery-item columns-4 lazy']" +
              "//img[contains(@src,'https://static.wixstatic.com/media')]")).getText();
      String parentId = innerText.substring(innerText.startsWith("static.wixstatic.com/media"),innerText.endsWith(".jpg"));
      String cartId =  innerText.substring(innerText.startsWith("static.wixstatic.com/media"),innerText.endsWith(".jpg"));*//*


    }
*/

}
